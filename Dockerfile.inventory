FROM node:18-alpine

RUN corepack enable && corepack prepare pnpm@latest --activate

WORKDIR /app

COPY . .

RUN pnpm install && pnpm --filter inventory build

CMD ["pnpm", "exec", "pm2-runtime", "start", "ecosystem.config.js", "--only", "inventory"]